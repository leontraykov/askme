<% background_class = "users-box-user-bg#{user.id % 5}" %>

<div class="users-box-user mr ml mb-sm">
  <a href="<%= user_path(user.nickname) %>">
    <%= image_tag user.gravatar_url, title: "@#{user.nickname}", class: "#{background_class} rounded" %>
  </a>
</div>
