<div class="container">
  <h2 class="title-lg mb">Question <%= @question.id %> edition</h2>

  <%= form_with model: @question do |form| %>
    <p><%= form.label :body, 'Question text' %></p>
    <p><%= form.text_area :body, rows: 5, class: 'form-text-area' %></p>
    <p class="mt"><%= form.label :user_id %></p>
    <p><%= form.text_field :user_id, class: 'form-text-field' %></p>
    <p class="mt"><%= form.submit 'Update this question', class: 'button button-main' %>
       OR 
    <%= link_to "Get back to the question", :back%></p> 
  <% end %>
</div>
